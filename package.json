{
  "name": "nouns-subgraph",
  "version": "0.1.0",
  "description": "A subgraph that indexes nouns events",
  "license": "GPL-3.0",
  "scripts": {
    "codegen": "graph codegen",
    "test": "graph test",
    "deploy-local": "graph create nouns --node http://localhost:8020/ && graph deploy nouns --node http://localhost:8020/ --ipfs http://localhost:5001/ --version-label=none"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.71.2",
    "@graphprotocol/graph-ts": "0.35.1",
    "matchstick-as": "0.6.0",
    "typescript": "5.4.5"
  }
}
